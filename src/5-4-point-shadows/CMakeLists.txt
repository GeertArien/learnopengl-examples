fips_begin_app(5-4-1-omnidirectional-depth windowed)
    fips_vs_warning_level(3)
    fips_files(1-omnidirectional-depth.c)
    sokol_shader(1-omnidirectional-depth.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(5-4-2-omnidirectional-shadows windowed)
    fips_vs_warning_level(3)
    fips_files(2-omnidirectional-shadows.c)
    sokol_shader(2-omnidirectional-shadows.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(5-4-3-omnidirectional-PCF windowed)
    fips_vs_warning_level(3)
    fips_files(3-omnidirectional-PCF.c)
    sokol_shader(3-omnidirectional-PCF.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()