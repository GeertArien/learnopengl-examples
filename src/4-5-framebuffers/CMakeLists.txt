fips_begin_app(4-5-1-render-to-texture windowed)
    fips_vs_warning_level(3)
    fips_files(1-render-to-texture.c)
    sokol_shader(1-render-to-texture.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-5-2-inversion windowed)
    fips_vs_warning_level(3)
    fips_files(2-inversion.c)
    sokol_shader(2-inversion.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-5-3-grayscale windowed)
    fips_vs_warning_level(3)
    fips_files(3-grayscale.c)
    sokol_shader(3-grayscale.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-5-4-sharpen windowed)
    fips_vs_warning_level(3)
    fips_files(4-sharpen.c)
    sokol_shader(4-sharpen.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-5-5-blur windowed)
    fips_vs_warning_level(3)
    fips_files(5-blur.c)
    sokol_shader(5-blur.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-5-6-edge-detection windowed)
    fips_vs_warning_level(3)
    fips_files(6-edge-detection.c)
    sokol_shader(6-edge-detection.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
