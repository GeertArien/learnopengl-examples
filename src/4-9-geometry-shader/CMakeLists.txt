# we're using texelFetch in the shader which is not supported in glsl100
if(slang STREQUAL "glsl300es:glsl100")
    set(slang "glsl300es")
endif()

fips_begin_app(4-9-1-lines windowed)
    fips_vs_warning_level(3)
    fips_files(1-lines.c)
    sokol_shader(1-lines.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-9-2-houses windowed)
    fips_vs_warning_level(3)
    fips_files(2-houses.c)
    sokol_shader(2-houses.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-9-3-exploding-object windowed)
    fips_vs_warning_level(3)
    fips_files(3-exploding-object.c)
    sokol_shader(3-exploding-object.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-9-4-visualizing-normals windowed)
    fips_vs_warning_level(3)
    fips_files(4-visualizing-normals.c)
    sokol_shader(4-visualizing-normals.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()