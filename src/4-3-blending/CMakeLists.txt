fips_begin_app(4-3-1-grass-opaque windowed)
    fips_vs_warning_level(3)
    fips_files(1-grass-opaque.c)
    sokol_shader(1-grass-opaque.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-3-2-grass-transparent windowed)
    fips_vs_warning_level(3)
    fips_files(2-grass-transparent.c)
    sokol_shader(2-grass-transparent.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-3-3-blending windowed)
    fips_vs_warning_level(3)
    fips_files(3-blending.c)
    sokol_shader(3-blending.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-3-4-blending-sorted windowed)
    fips_vs_warning_level(3)
    fips_files(4-blending-sorted.c)
    sokol_shader(4-blending-sorted.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
