fips_begin_app(4-1-1-depth-always windowed)
    fips_vs_warning_level(3)
    fips_files(1-depth-always.c)
    sokol_shader(1-depth-always.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-1-2-depth-less windowed)
    fips_vs_warning_level(3)
    fips_files(2-depth-less.c)
    sokol_shader(2-depth-less.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-1-3-depth-buffer windowed)
    fips_vs_warning_level(3)
    fips_files(3-depth-buffer.c)
    sokol_shader(3-depth-buffer.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-1-4-linear-depth-buffer windowed)
    fips_vs_warning_level(3)
    fips_files(4-linear-depth-buffer.c)
    sokol_shader(4-linear-depth-buffer.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
