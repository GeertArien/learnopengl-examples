fips_begin_app(5-3-1-mapping-depth windowed)
    fips_vs_warning_level(3)
    fips_files(1-mapping-depth.c)
    sokol_shader(1-mapping-depth.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(5-3-2-rendering-shadows windowed)
    fips_vs_warning_level(3)
    fips_files(2-rendering-shadows.c)
    sokol_shader(2-rendering-shadows.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(5-3-3-improved-shadows windowed)
    fips_vs_warning_level(3)
    fips_files(3-improved-shadows.c)
    sokol_shader(3-improved-shadows.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()