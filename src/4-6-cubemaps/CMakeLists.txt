fips_begin_app(4-6-1-skybox windowed)
    fips_vs_warning_level(3)
    fips_files(1-skybox.c)
    sokol_shader(1-skybox.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-6-2-reflection-cube windowed)
    fips_vs_warning_level(3)
    fips_files(2-reflection-cube.c)
    sokol_shader(2-reflection-cube.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-6-3-reflection-backpack windowed)
    fips_vs_warning_level(3)
    fips_files(3-reflection-backpack.c)
    sokol_shader(3-reflection-backpack.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-6-4-refraction-cube windowed)
    fips_vs_warning_level(3)
    fips_files(4-refraction-cube.c)
    sokol_shader(4-refraction-cube.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-6-5-refraction-backpack windowed)
    fips_vs_warning_level(3)
    fips_files(5-refraction-backpack.c)
    sokol_shader(5-refraction-backpack.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
