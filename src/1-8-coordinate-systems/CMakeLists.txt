fips_begin_app(1-8-1-plane windowed)
    fips_vs_warning_level(3)
    fips_files(1-plane.c)
    sokol_shader(shaders.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(1-8-2-cube windowed)
    fips_vs_warning_level(3)
    fips_files(2-cube.c)
    sokol_shader(shaders.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(1-8-3-more-cubes windowed)
    fips_vs_warning_level(3)
    fips_files(3-more-cubes.c)
    sokol_shader(shaders.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
