fips_begin_app(4-11-1-msaa windowed)
    fips_vs_warning_level(3)
    fips_files(1-msaa.c)
    sokol_shader(1-msaa.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-11-2-offscreen-msaa windowed)
    fips_vs_warning_level(3)
    fips_files(2-offscreen-msaa.c)
    sokol_shader(2-offscreen-msaa.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-11-3-grayscale-msaa windowed)
    fips_vs_warning_level(3)
    fips_files(3-grayscale-msaa.c)
    sokol_shader(3-grayscale-msaa.glsl ${slang})
    fips_deps(sokol)
fips_end_app()
