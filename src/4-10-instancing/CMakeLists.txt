fips_begin_app(4-10-1-instancing windowed)
    fips_vs_warning_level(3)
    fips_files(1-instancing.c)
    sokol_shader(1-instancing.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-10-2-instanced-arrays windowed)
    fips_vs_warning_level(3)
    fips_files(2-instanced-arrays.c)
    sokol_shader(2-instanced-arrays.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-10-3-asteroid-field windowed)
    fips_vs_warning_level(3)
    fips_files(3-asteroid-field.c)
    sokol_shader(3-asteroid-field.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-10-4-asteroid-field-instanced windowed)
    fips_vs_warning_level(3)
    fips_files(4-asteroid-field-instanced.c)
    sokol_shader(4-asteroid-field-instanced.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
