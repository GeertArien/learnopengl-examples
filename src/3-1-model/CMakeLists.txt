fips_begin_app(3-1-1-backpack-diffuse windowed)
    fips_vs_warning_level(3)
    fips_files(1-backpack-diffuse.c)
    sokol_shader(1-backpack-diffuse.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(3-1-2-backpack-lights windowed)
    fips_vs_warning_level(3)
    fips_files(2-backpack-lights.c)
    sokol_shader(2-backpack-lights.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
