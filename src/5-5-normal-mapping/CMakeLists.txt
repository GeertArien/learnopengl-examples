fips_begin_app(5-5-1-normal-mapping windowed)
    fips_vs_warning_level(3)
    fips_files(1-normal-mapping.c)
    sokol_shader(1-normal-mapping.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(5-5-2-tangent-space windowed)
    fips_vs_warning_level(3)
    fips_files(2-tangent-space.c)
    sokol_shader(2-tangent-space.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(5-5-3-complex-object windowed)
    fips_vs_warning_level(3)
    fips_files(3-complex-object.c)
    sokol_shader(3-complex-object.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
