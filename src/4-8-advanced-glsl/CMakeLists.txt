fips_begin_app(4-8-1-point-size windowed)
    fips_vs_warning_level(3)
    fips_files(1-point-size.c)
    sokol_shader(1-point-size.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-8-2-frag-coord windowed)
    fips_vs_warning_level(3)
    fips_files(2-frag-coord.c)
    sokol_shader(2-frag-coord.glsl ${slang})
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-8-3-front-facing windowed)
    fips_vs_warning_level(3)
    fips_files(3-front-facing.c)
    sokol_shader(3-front-facing.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(4-8-4-uniform-buffers windowed)
    fips_vs_warning_level(3)
    fips_files(4-uniform-buffers.c)
    sokol_shader(4-uniform-buffers.glsl ${slang})
    fips_deps(sokol)
fips_end_app()