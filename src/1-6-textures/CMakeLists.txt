fips_begin_app(1-6-1-texture windowed)
    fips_vs_warning_level(3)
    fips_files(1-texture.c)
    sokol_shader(1-texture.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(1-6-2-texture-blend windowed)
    fips_vs_warning_level(3)
    fips_files(2-texture-blend.c)
    sokol_shader(2-texture-blend.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()

fips_begin_app(1-6-3-multiple-textures windowed)
    fips_vs_warning_level(3)
    fips_files(3-multiple-textures.c)
    sokol_shader(3-multiple-textures.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
