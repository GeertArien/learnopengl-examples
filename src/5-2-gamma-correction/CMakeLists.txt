fips_begin_app(5-2-1-gamma-correction windowed)
    fips_vs_warning_level(3)
    fips_files(1-gamma-correction.c)
    sokol_shader(1-gamma-correction.glsl ${slang})
    fipsutil_copy(textures-assets.yml)
    fips_deps(sokol)
fips_end_app()
